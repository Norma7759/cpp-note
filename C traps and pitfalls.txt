1.注意=和==，以及&和&&，|和||的区别。
2.符号匹配的贪心算法：从左到右每一个符号包含尽可能多的字符。
3.0开头的数字会导致误认为八进制数。
4.字符指针应该用双引号赋值，等价于数组。
5.运算符优先级问题。
6.switch break遗漏。
7.函数调用()。不加()则只计算函数地址。
8.如果有多个if，else只和最近一个if结合。
9.二维数组的首地址应该是指向数组的指针
10.