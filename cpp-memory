代码区：存放函数的二进制代码，由操作系统管理。共享，只读。
全局区：全局变量（global），静态变量（static），常量（字符串，const）。由操作系统释放。
栈区：编译器分配和释放，存放函数的参数，局部变量等。
堆区：程序员分配和释放，程序结束时由操作系统回收。


type* p = new tpye()；
new会返回一个该类型的指针
delete p  //释放内存
delete[] p //释放数组